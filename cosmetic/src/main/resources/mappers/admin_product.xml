<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin_product">


<insert id="product_insert">
insert into product (p_name, p_price, p_stock, p_img1, p_detail, p_cate)
values(#{p_name},#{p_price}, #{p_stock}, #{p_img1},#{p_detail}, #{p_cate})
</insert>

<!-- <insert id="product_option_insert">
insert into product_option (o_p_id, o_price, o_name, o_amount, o_id)
values(#{},)

</insert>
 -->
 
 <select id="big" resultType="String" >
		select DISTINCT ctg_big
		from ctg
</select>





</mapper>